# Production environment variables
# Cloudflare Worker API URL (update after deployment)
VITE_API_URL=https://vam-insurance-api.your-subdomain.workers.dev

# Instructions:
# 1. Deploy Cloudflare Worker first: cd CloudflareWorker && wrangler deploy
# 2. Copy the Worker URL from deployment output
# 3. Update VITE_API_URL above with your actual Worker URL
# 4. Redeploy frontend on Vercel (automatic via GitHub push)
